---
- name: Apply common OS hardening
  hosts: all
  become: yes
  roles:
    - common

- name: Initialiser Docker Swarm sur le Manager
  hosts: managers
  become: yes
  tasks:
    - name: Vérifier si Swarm est déjà actif
      shell: docker info --format '{% raw %}{{ .Swarm.LocalNodeState }}{% endraw %}'
      register: swarm_state
      changed_when: false

    - name: Initialiser le cluster Swarm
      shell: docker swarm init --advertise-addr {{ ansible_default_ipv4.address }}
      when: swarm_state.stdout != 'active'

    - name: Récupérer le token pour les workers
      shell: docker swarm join-token -q worker
      register: worker_token
      changed_when: false

- name: Rejoindre le Swarm (Workers)
  hosts: workers_app:workers_data
  become: yes
  tasks:
    - name: Vérifier l'état Swarm du worker
      shell: docker info --format '{% raw %}{{ .Swarm.LocalNodeState }}{% endraw %}'
      register: worker_swarm_state
      changed_when: false

    - name: Rejoindre le cluster Swarm
      shell: >
        docker swarm join 
        --token {{ hostvars[groups['managers'][0]]['worker_token']['stdout'] }} 
        {{ hostvars[groups['managers'][0]]['ansible_default_ipv4']['address'] }}:2377
      when: worker_swarm_state.stdout != 'active'

- name: Appliquer les labels de placement sur les nœuds
  hosts: managers
  become: yes
  tasks:
    - name: Label tier=app pour workers_app
      shell: docker node update --label-add tier=app {{ item }}
      loop: "{{ groups['workers_app'] | default([]) }}"
      ignore_errors: yes 

    - name: Label tier=data pour workers_data
      shell: docker node update --label-add tier=data {{ item }}
      loop: "{{ groups['workers_data'] | default([]) }}"
      ignore_errors: yes

- name: Deploy AgileFlow Stack
  hosts: managers
  become: yes
  vars:
    stack_name: agileflow
    compose_src: ../docker-compose.prod.yml
    compose_dest: /opt/agileflow/docker-compose.yml

  tasks:
    - name: Create destination directory
      file:
        path: /opt/agileflow
        state: directory
        mode: '0755'

    - name: Copy docker-compose to manager
      copy:
        src: "{{ compose_src }}"
        dest: "{{ compose_dest }}"
        mode: '0644'

    - name: Deploy Swarm stack
      shell: >
        docker stack deploy 
        --with-registry-auth 
        -c {{ compose_dest }} 
        {{ stack_name }}